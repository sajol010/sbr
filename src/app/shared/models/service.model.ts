export interface Service {
  id: number | string;
  name: string;
  // Add other relevant service properties if needed later, e.g.:
  // duration?: number; // in minutes
  // price?: number;
  // description?: string;
}
